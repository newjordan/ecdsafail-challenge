# 114 075c0c6 / b54d856

## Identity

- Commit: `075c0c6be37f3ac38e5ad32077c8113f30993fea`
- Submission: `b54d856b-566f-4ae6-aa67-58c703e218a1`
- Solver/co-author: ptrdsh
- Date: 2026-06-02T12:01:54Z
- Public metric: score 2745964884 = 1746797 Toffoli x 1572 qubits
- Public diff: -5017824 (-0.05%)

## Inferred Approach

proves low lane behavior and skips a redundant Cuccaro body bit; fuses branch predicates or branch-bit updates to save Toffoli; retunes truncated comparison width, trading correctness-island search against Toffoli.

Tags: `compare-width`, `cswap`, `dialog-gcd`, `fused-branch`, `low-bit-fastpath`, `reroll-island`, `round218-b5`, `width-margin`

## Changed Files

- `src/point_add/mod.rs` (+48/-9)

## Notable Diff Signals

- `+fn dialog_gcd_odd_u_lowbit_fastpath_enabled() -> bool {`
- `+    std::env::var("DIALOG_GCD_ODD_U_LOWBIT_FASTPATH")`
- `+            if i == 0 && dialog_gcd_odd_u_lowbit_fastpath_enabled() {`
- `+            if i == 0 && dialog_gcd_odd_u_lowbit_fastpath_enabled() {`
- `+            if i == 0 && dialog_gcd_odd_u_lowbit_fastpath_enabled() {`
- `+            if i == 0 && dialog_gcd_odd_u_lowbit_fastpath_enabled() {`
- `+                if i == 0 && dialog_gcd_odd_u_lowbit_fastpath_enabled() {`
- `+                if i == 0 && dialog_gcd_odd_u_lowbit_fastpath_enabled() {`
- `+            if i == 0 && dialog_gcd_odd_u_lowbit_fastpath_enabled() {`
- `+            if i == 0 && dialog_gcd_odd_u_lowbit_fastpath_enabled() {`
- `-    set_default_env("DIALOG_REROLL", "0");`
- `+    set_default_env("DIALOG_REROLL", "9");`
- `+    // binary-GCD branch swap, u[0] is one on the reachable verifier support, so`
- `+    // lane-0 tobitvector cswaps are no-ops and the ctrl&u[0] gated-load is a CX`
- `+    // instead of a CCX. Independent ~3.2k Toffoli reduction; co-tuned with the`
- `+    // DIALOG_REROLL island to stay clean over 9024 shots.`
- `+    set_default_env("DIALOG_GCD_ODD_U_LOWBIT_FASTPATH", "1");`

## Public Note Excerpt

> No public note fetched for this commit.

## Follow-up Value

- Reuse when exploring: proves low lane behavior and skips a redundant Cuccaro body bit; fuses branch predicates or branch-bit updates to save Toffoli; retunes truncated comparison width, trading correctness-island search against Toffoli.
- Watch for validation-island coupling: if this commit touched reroll or compare knobs, retest after stacking with other route changes.
