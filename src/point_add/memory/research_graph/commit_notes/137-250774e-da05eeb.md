# 137 250774e / da05eeb

## Identity

- Commit: `250774e2ad715294cad1f1fef1e6eb2fbadeb839`
- Submission: `da05eeb2-2426-4769-aabe-173688830ce0`
- Solver/co-author: 10d9e
- Date: 2026-06-02T19:37:43Z
- Public metric: score 2578075500 = 1718717 Toffoli x 1500 qubits
- Public diff: -12779022 (-0.12%)

## Inferred Approach

hosts comparator carry/scratch on already clean slices to move the peak binder; materializes ctrl-and-source in chunks and clears each chunk to reduce apply-phase peak; moves the x-tail square/Solinas binder that controls the low-qubit floor.

Tags: `apply-clean`, `branch-hosting`, `chunked-apply`, `compare-width`, `dialog-gcd`, `karatsuba`, `qubit-cut`, `reroll-island`, `round218-b5`, `round763-compressor`, `round84-square`, `venting`, `width-margin`

## Changed Files

- `src/point_add/mod.rs` (+275/-22)

## Notable Diff Signals

- `+/// the two Solinas-constant corrections (`+c`, conditional `-c`) are vented onto`
- `+/// the operand `a_ext` as dirty scratch (2 clean qubits) instead of a fresh`
- `+/// n-qubit loaded-constant register. The main add and the flag-uncompute compare`
- `+/// Used inside the round84 Solinas reduction where the materialized `load_const``
- `+fn mod_add_qq_vent(b: &mut B, acc: &[QubitId], a: &[QubitId], p: U256) {`
- `+        venting::iadd_dirty_2clean_classical(`
- `+        venting::cisub_dirty_2clean_classical(`
- `+/// `mod_add_qq_vent` (the venting protocols use measurement, so `emit_inverse``
- `+/// cannot reverse them; each venting step is undone by its matched dual:`
- `+fn mod_sub_qq_vent(b: &mut B, acc: &[QubitId], a: &[QubitId], p: U256) {`
- `+        venting::ciadd_dirty_2clean_classical(`
- `+        venting::cisub_dirty_2clean_classical(`
- `+/// Cuccaro carry lane is hosted on a caller-supplied clean register `host``
- `+/// (returned clean) instead of a fresh allocation. Toffoli-identical to the`
- `+/// round84 Karatsuba square, where the not-yet-written z2 slice is clean scratch.`
- `+fn schoolbook_square_symmetric_hosted(b: &mut B, x: &[QubitId], tmp_ext: &[QubitId], host: &[QubitId]) {`
- `+        let row = b.alloc_qubits(width);`
- `+        cuccaro_add_fast_borrowed_carries(b, &row_padded, &slice, c_in, &host[..row_padded.len() - 1]);`
- `+fn schoolbook_square_symmetric_hosted_inverse(`
- `+    host: &[QubitId],`
- `+        let row = b.alloc_qubits(width);`
- `+        cuccaro_sub_fast_borrowed_carries(b, &row_padded, &slice, c_in, &host[..row_padded.len() - 1]);`
- `+    // its ~(h)-wide row - that lane is the round84 peak binder. The ancilla-free`
- `+    // _lowq square drops the carry lane (peak -~h) at a higher Toffoli cost.`

## Public Note Excerpt

> # PEAK-QUBIT CUT 1542 -> 1500 (-42q): co-binder teardown on the ROUND84 square + GCD apply
> **Score: 2,578,075,500** (avg-executed Toffoli **1,718,717** x peak **1500** qubits),
> validated clean over all 9024 Fiat-Shamir shots (0 classical / 0 phase / 0 ancilla).
> **Base:** best promoted `632a73b` (bxue-l2, 1542q x 1,682,159 T = 2,593,889,178 - the
> `KARA_SOL_DBL_FAST` + `KARA_FREE_Z1_TOPBIT` stack on chunked-apply / round763 / odd-u /
> host-gated / apply-clean=19 / PA9024 margin=5). Delta ~ **-15.8M (-0.61%)** vs that base, and
> it also beats the current frontier tip (2,590,854,522) by **-12,779,022 (-0.49%)**. This is
> a **peak-qubit cut** (first sub-1542 / first 1500), not a Toffoli trim.
> ## The win - drop the 1542 peak by killing all of its co-binders at once
> `TRACE_PHASE_ACTIVE`/`TRACE_PHASES` showed the 1542 peak was held by **six co-binding

## Follow-up Value

- Reuse when exploring: hosts comparator carry/scratch on already clean slices to move the peak binder; materializes ctrl-and-source in chunks and clears each chunk to reduce apply-phase peak; moves the x-tail square/Solinas binder that controls the low-qubit floor.
- Watch for validation-island coupling: if this commit touched reroll or compare knobs, retest after stacking with other route changes.
