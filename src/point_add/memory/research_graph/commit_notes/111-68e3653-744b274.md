# 111 68e3653 / 744b274

## Identity

- Commit: `68e36539833108ef97eb4692304640faaaef8509`
- Submission: `744b2747-71aa-4d4a-9034-194f69e7352e`
- Solver/co-author: saucegodbased
- Date: 2026-06-02T10:54:36Z
- Public metric: score 2783850084 = 1770897 Toffoli x 1572 qubits
- Public diff: -38648718 (-0.36%)

## Inferred Approach

hosts comparator carry/scratch on already clean slices to move the peak binder; moves the x-tail square/Solinas binder that controls the low-qubit floor; retunes truncated comparison width, trading correctness-island search against Toffoli.

Tags: `branch-hosting`, `compare-width`, `dialog-gcd`, `qubit-cut`, `reroll-island`, `round84-square`, `width-margin`

## Changed Files

- `src/point_add/mod.rs` (+257/-16)

## Notable Diff Signals

- `+/// once (vs n-1 for a single `cuccaro_add_fast`), at ~1 Toffoli/bit - this is`
- `+/// block's borrow-out). ~n/blocks measured-borrow ancillae live; ~1 Toffoli/bit.`
- `+            let a = b.alloc_qubits(n);`
- `+            let acc = b.alloc_qubits(n);`
- `+            let sa = bs.alloc_qubits(n);`
- `+            let sacc = bs.alloc_qubits(n);`
- `+    if let Some(w) = dialog_gcd_apply_window_blocks() {`
- `+fn dialog_gcd_apply_window_blocks() -> Option<usize> {`
- `+    std::env::var("DIALOG_GCD_APPLY_WINDOW_BLOCKS")`
- `+        if let Some(w) = dialog_gcd_apply_window_blocks() {`
- `-    set_default_env("DIALOG_GCD_PA9024_COMPARE_SCHEDULE_MARGIN", "5");`
- `+    set_default_env("DIALOG_GCD_PA9024_COMPARE_SCHEDULE_MARGIN", "8");`
- `-    // 27 and co-tune the Fiat-Shamir reroll to land a clean 9024-shot island.`
- `-    // Pure Toffoli reduction; margin 28 -> 27 saves -4,636 (1,667,601 ->`
- `-    // 1,662,965), peak-neutral at 1698, paired with REROLL=2 below.`
- `-    // (Validated 0/0/0 over 9024 via eval_circuit; reroll island re-scanned for`
- `-    // the current baked default stack - margin=27 needs REROLL=2, not 29.)`
- `-    set_default_env("DIALOG_GCD_WIDTH_MARGIN", "27");`
- `+    // co-tune the Fiat-Shamir reroll to land a clean 9024-shot island. Pure`
- `+    // Toffoli reduction (2447846 -> 2396158), peak-neutral at 1698.`
- `+    set_default_env("DIALOG_GCD_WIDTH_MARGIN", "28");`
- `-    // Pure Toffoli reduction (1697569 -> 1673629), peak-neutral at 1698.`
- `-    // REROLL re-scanned for the WIDTH_MARGIN=27 island: REROLL=2 lands 0/0/0`
- `-    // over 9024 (29 was the margin=28 island). Score 2,823,714,570.`

## Public Note Excerpt

> FIRST sub-1698 qubit cut: 1698 -> 1572 peak (-126 qubits) at near-flat Toffoli. Two orthogonal levers, both seed-independent: (1) DIALOG_GCD_HOST_GATED hosts the GCD-body materialized 'gated' register on idle future-log slots (0 Toffoli); (2) windowed apply add/sub -- the 256-wide cuccaro carry lane is split into 2 blocks with Gidney measurement-uncompute and a measured (cmp_lt_into_fast) boundary-carry clear, so the carry lane never coexists full-width with the materialized f at the apply peak. The apply transient drops below the squarer floor; peak binds at 1572. Toffoli 1,668,753 -> 1,770,897 (+102k for the boundary clears), peak 1698 -> 1572 => score 2,833,542,594 -> 2,783,850,084. Validated 0 classical / 0 phase / 0 ancilla over 9024 shots.

## Follow-up Value

- Reuse when exploring: hosts comparator carry/scratch on already clean slices to move the peak binder; moves the x-tail square/Solinas binder that controls the low-qubit floor; retunes truncated comparison width, trading correctness-island search against Toffoli.
- Watch for validation-island coupling: if this commit touched reroll or compare knobs, retest after stacking with other route changes.
