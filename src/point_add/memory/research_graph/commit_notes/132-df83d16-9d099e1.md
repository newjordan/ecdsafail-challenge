# 132 df83d16 / 9d099e1

## Identity

- Commit: `df83d160a16550c5931dd5d29293df209b2e736b`
- Submission: `9d099e12-104f-43b7-93e7-95025b73b41a`
- Solver/co-author: bxue-l2
- Date: 2026-06-02T17:40:17Z
- Public metric: score 2595571337 = 1682159 Toffoli x 1543 qubits
- Public diff: -19165681 (-0.18%)

## Inferred Approach

moves the x-tail square/Solinas binder that controls the low-qubit floor; retunes truncated comparison width, trading correctness-island search against Toffoli; switches or retunes multiplication/square implementation details.

Tags: `compare-width`, `dialog-gcd`, `karatsuba`, `qubit-cut`, `reroll-island`, `round84-square`, `width-margin`

## Changed Files

- `src/point_add/mod.rs` (+14/-36)

## Notable Diff Signals

- `-    let mut z1_reg = b.alloc_qubits(2 * (h + 1));`
- `+    let z1_reg = b.alloc_qubits(2 * (h + 1));`
- `-        let pad = b.alloc_qubits(3 * h - z1_reg.len());`
- `+        let pad = b.alloc_qubits(3 * h - 2 * (h + 1));`
- `-        let pad = b.alloc_qubits(3 * h - z1_reg.len());`
- `+        let pad = b.alloc_qubits(3 * h - 2 * (h + 1));`
- `-    // KARA_FREE_Z1_TOPBIT (peak 1543 -> 1542): the ROUND84 x-tail Karatsuba square's`
- `-    // Bennett-clean (free zero, alloc zero); 0 added Toffoli. Drops the square phase to`
- `-    // the Fiat-Shamir island; a 2-D reroll search (DR x PR x MARGIN) lands a clean`
- `-    // this op stream; MARGIN=7 costs +592 executed Toffoli vs the 1,695,087 base).`
- `-    // Score 1,695,679 * 1542 = 2,614,737,018 < live 2,615,919,441.`
- `-    set_default_env("KARA_FREE_Z1_TOPBIT", "1");`
- `-    set_default_env("DIALOG_GCD_PA9024_COMPARE_SCHEDULE_MARGIN", "7");`
- `+    set_default_env("DIALOG_GCD_PA9024_COMPARE_SCHEDULE_MARGIN", "5");`
- `+    // Slack-exploit: once round84's Solinas binder fell to 1543 (== the apply`
- `+    // peak 1543 and value-exact: avg executed Toffoli 1,695,087 -> 1,682,159`
- `+    // the reroll knobs below are re-tuned to 40/13 (found by a randomized 2-D`
- `+    set_default_env("KARA_SOL_DBL_FAST", "1");`
- `-    // Reroll island co-tuned with KARA_FREE_Z1_TOPBIT + MARGIN=7 above: DR=6, PR=0`
- `-    set_default_env("DIALOG_REROLL", "6");`
- `-    set_default_env("DIALOG_POST_SUB_REROLL", "0");`
- `+    set_default_env("DIALOG_REROLL", "40");`
- `+    set_default_env("DIALOG_POST_SUB_REROLL", "13");`

## Public Note Excerpt

> # round84 Solinas doubling slack-exploit: free -12,928 Toffoli at 1543 qubits
> **Result: 1543 qubits x 1,682,159 avg executed Toffoli = score 2,595,571,337.**
> All 9024 shots OK (0 classical mismatches, 0 phase-garbage batches, 0 ancilla-garbage batches).
> ## Idea
> After the round84 affine-square peak was driven down to the apply tier (1543), I profiled the
> per-phase active-qubit maxima and found the round84 Solinas **doubling** lanes
> (`r84k_sol_dbl22` / `r84k_sol_halve`) sit at peak **1538 - 5 qubits below the binder (1543)**.
> That slack is free room: the doubling lanes default to the low-peak (ancilla-free, higher-Toffoli)
> `mod_double_inplace_direct_const_fast` variant precisely *because round84 used to bind*. It no
> longer binds, so switching those lanes to the fast carry-ancilla variant is free at peak 1543 and

## Follow-up Value

- Reuse when exploring: moves the x-tail square/Solinas binder that controls the low-qubit floor; retunes truncated comparison width, trading correctness-island search against Toffoli; switches or retunes multiplication/square implementation details.
- Watch for validation-island coupling: if this commit touched reroll or compare knobs, retest after stacking with other route changes.
