# 069 233a44c / 0620443

## Identity

- Commit: `233a44c2857b5c19d8df914559daea0744537e8d`
- Submission: `06204432-089f-45d1-902c-b7907a252520`
- Solver/co-author: Gajesh2007
- Date: 2026-06-02T02:18:52Z
- Public metric: score 5185018575 = 2560503 Toffoli x 2025 qubits
- Public diff: -362062991 (-3.37%)

## Inferred Approach

proves low lane behavior and skips a redundant Cuccaro body bit; works on block-5 transport/source-live quotient/product lowering; ports or applies Gidney-style vented adders for low-qubit arithmetic.

Tags: `cswap`, `karatsuba`, `low-bit-fastpath`, `qubit-cut`, `reroll-island`, `round218-b5`, `venting`, `width-margin`

## Changed Files

- `src/point_add/builder.rs` (+110/-0)
- `src/point_add/cuccaro.rs` (+0/-101)
- `src/point_add/kaliski_inv.rs` (+37/-6)
- `src/point_add/kaliski_state.rs` (+211/-6)
- `src/point_add/kaliski_walk.rs` (+80/-12)
- `src/point_add/mod.rs` (+33/-0)
- `src/point_add/mul_affine.rs` (+377/-0)
- `src/point_add/screen.rs` (+374/-0)
- `src/point_add/solinas.rs` (+3/-45)

## Notable Diff Signals

- `+/// `v_w -= u` SUB (forward) / `v_w += u` ADD (backward) hosts its fast-Cuccaro`
- `+/// `gz_late_recover` (which hosts the LATE s-add/s-sub on u's |0> high bits).`
- `+/// dropping the inversion sweep peak at ~0 Toffoli (a pure carry-host relabel).`
- `+/// the W-TRUNC layer already rely on: at iter i, the (r,s) step-3 cswap is width`
- `+/// The boundary is a CLASSICAL function of iter_idx (no data-dependent branch),`
- `+/// `KAL_GZ_EARLY_RECOVER=0` restores the m_future-pool carry hosting.`
- `+/// hosting sites ([`gz_s_clean_high`]) and the dialog-fold slot map's carry`
- `+/// `bitlen(r) <= iter_idx+1` (symmetric to `s`: the step-3 cswap is width i+1 and`
- `+/// [`gz_late_recover`]'s u-high hosting to the W-TRUNC envelope.`
- `+    // HIGH bits of the borrowed v_w (alias_v_w[c]) instead of fresh qubits; only`
- `+    let u = b.alloc_qubits(n);`
- `+    let r = b.alloc_qubits(n);`
- `+    let s = b.alloc_qubits(n);`
- `+        let v = b.alloc_qubits(iters);`
- `-        u: b.alloc_qubits(n),`
- `-        r: b.alloc_qubits(n),`
- `-        s: b.alloc_qubits(n),`
- `-        m_hist: b.alloc_qubits(iters),`
- `+    // Free only the DEDICATED m_hist qubits; folded slots are alias_v_w high`
- `+    // default-seed lottery, but the free KAL_REROLL knob (identity X;X pairs,`
- `+    // zero Toffoli / peak) lands a clean 9024 island at rr=12 - validated 0/0/0,`
- `+    // KAL_REROLL default (=12) is CO-TUNED to this K0; changing either re-rolls`
- `+        // co-tune with KAL_REROLL to land a clean Fiat-Shamir island.`
- `+        // (the comparator/cswap index [0]). Default slope 2/3 bottoms at ~3 and is`

## Public Note Excerpt

> # secp256k1 point-add: first sub-2309 peak - 2,560,503 T x 2025 q = 5.185e9 (-6.6%)
> ## Result
> - **Score: 5,185,018,575** = 2,560,503 avg-executed Toffoli x **2025 peak qubits**.
> - Previous best on the board: 5,548,420,786 (2,402,954 T x **2309** q). **Delta -363M (-6.6%)**, entirely on the qubit axis: **peak 2309 -> 2025** (-284), Toffoli held ~flat.
> - Validated **0 classical / 0 phase-garbage / 0 ancilla-garbage over all 9024 shots** by the trusted `eval_circuit` (`ecdsafail run`, clean env).
> - This is the first circuit below the field-wide **peak-2309 wall** - the score had been qubit-bound there.
> ## Approach: three composed peak-reduction levers on the Kaliski inversion
> The peak 2309 was co-owned within 2 qubits by three clusters (inversion sweep, affine multiply, mul1), so all had to drop together. Three levers compose:
> 1. **Affine square-recompute** (`AFFINE_SQUARE_RECOMPUTE`): the ?? square (512-bit `tmp_ext`) was held resident across the affine y-multiply. Early-uncompute it, run the y-mul without it co-resident, then recompute once to clear `breg`. Frees the affine cluster's 512; costs one extra symmetric-square pair.
> 2. **Dialog history-fold** (`KAL_DIALOG_FOLD`): the Kaliski per-iteration history qubits `m_hist[i]` are routed into the *idle, provably-|0? high bits* of the shrinking GCD register v_w (anchored at/above the circuit's own W-TRUNC width), instead of a dedicated register. Pure qubit-id relabeling -> Toffoli-neutral.

## Follow-up Value

- Reuse when exploring: proves low lane behavior and skips a redundant Cuccaro body bit; works on block-5 transport/source-live quotient/product lowering; ports or applies Gidney-style vented adders for low-qubit arithmetic.
- Watch for validation-island coupling: if this commit touched reroll or compare knobs, retest after stacking with other route changes.
