# 019 9bcc27d / d35d6e7

## Identity

- Commit: `9bcc27d76b7a59ef27f964dc85ea7a452593c5f4`
- Submission: `d35d6e7c-aa10-48ce-84af-67331506472e`
- Solver/co-author: Gajesh2007
- Date: 2026-05-31T11:46:31Z
- Public metric: score 9604339032 = 3546654 Toffoli x 2708 qubits
- Public diff: -714197088 (-6.64%)

## Inferred Approach

moves the x-tail square/Solinas binder that controls the low-qubit floor; switches or retunes multiplication/square implementation details; primarily targets peak qubit width rather than raw Toffoli.

Tags: `cswap`, `karatsuba`, `qubit-cut`, `round84-square`

## Changed Files

- `src/point_add/mod.rs` (+289/-166)

## Notable Diff Signals

- `-// Exhaustive-audit Toffoli wins (2026-05-30). Each defaults ON once verified.`
- `-// become the low-scratch fast variants - same operands, fewer Toffoli, fewer`
- `-// applied to the pair1 sister mod_mul_write_into_zero_acc_karatsuba_with_tmp_ext`
- `-    // site (after shift_left, with extra locals alive) to save 256 carry qubits.`
- `+    // to save 256 carry qubits at the expense of ~n Toffoli.`
- `+/// (r,s) cswap boundary-merge: defer step9(k) and fuse it with step3(k+1) on`
- `+/// `cswap(p)?cswap(q) = cswap(p?q)`. A persistent `frame` parity qubit carries`
- `+/// Default ON; `KAL_CSWAP_RS_MERGE=0` restores the byte-identical eager path.`
- `+fn kal_cswap_rs_merge_enabled() -> bool {`
- `+    std::env::var("KAL_CSWAP_RS_MERGE").ok().as_deref() != Some("0")`
- `+fn kal_cswap_rs_merge_generic_enabled() -> bool {`
- `+    kal_cswap_rs_merge_enabled()`
- `+        && std::env::var("KAL_CSWAP_RS_MERGE_GENERIC").ok().as_deref() != Some("0")`
- `+fn kal_cswap_rs_merge_bulk_enabled() -> bool {`
- `+    kal_cswap_rs_merge_enabled()`
- `+        && std::env::var("KAL_CSWAP_RS_MERGE_BULK").ok().as_deref() != Some("0")`
- `+    // (r,s) cswap boundary-merge is only valid on the default coeff=None channel.`
- `+    let merge_rs = coeff.is_none() && kal_cswap_rs_merge_bulk_enabled();`
- `-        cswap(b, a_f, r[j], s[j]);`
- `+        // Merged cswap control = a_{k-1} ? a_k. Build into a_f (free CX),`
- `+        // emit one cswap (width = min(k+1,n) = step9(k-1) width = step3(k)`
- `+            cswap(b, a_f, r[j], s[j]);`
- `+            cswap(b, a_f, r[j], s[j]);`
- `-        cswap(b, a_f, r[j], s[j]);`

## Public Note Excerpt

> ## C1: (r,s) cswap boundary-merge
> Score 9,604,339,032 (3,546,654 avg executed Toffoli x 2708 peak qubits); benchmark.sh all 9024 Fiat-Shamir shots clean (0 mismatches, 0 phase-garbage, 0 ancilla-garbage). ~-9.2% vs the 10.58e9 baseline. Kaliski binary-GCD inverse: the (r,s) Bezout cswaps of step9(k) and step3(k+1) merge across the iteration boundary via cswap(a_k)*cswap(a_{k+1}) carried by one frame parity qubit; -274k Toffoli, peak-neutral, phase-preserving. Gate KAL_CSWAP_RS_MERGE default-on.

## Follow-up Value

- Reuse when exploring: moves the x-tail square/Solinas binder that controls the low-qubit floor; switches or retunes multiplication/square implementation details; primarily targets peak qubit width rather than raw Toffoli.
- Watch for validation-island coupling: if this commit touched reroll or compare knobs, retest after stacking with other route changes.
