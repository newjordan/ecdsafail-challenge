# 113 b6d6f41 / e29e120

## Identity

- Commit: `b6d6f41a2cb8e796c93792a76524c40cc86c9bf2`
- Submission: `e29e1200-20c3-43c6-a5b4-ab2436d5970e`
- Solver/co-author: mpjunior92
- Date: 2026-06-02T11:38:10Z
- Public metric: score 2750982708 = 1749989 Toffoli x 1572 qubits
- Public diff: -18235200 (-0.17%)

## Inferred Approach

moves the x-tail square/Solinas binder that controls the low-qubit floor; switches or retunes multiplication/square implementation details; tightens active-width envelope/margin for the dialog-GCD body.

Tags: `dialog-gcd`, `karatsuba`, `qubit-cut`, `reroll-island`, `round84-square`, `width-margin`

## Changed Files

- `src/point_add/mod.rs` (+14/-8)

## Notable Diff Signals

- `-    set_default_env("ROUND84_XTAIL_SCHOOLBOOK", "1");`
- `-    // co-tune the Fiat-Shamir reroll to land a clean 9024-shot island. Pure`
- `-    // Toffoli reduction (2447846 -> 2396158), peak-neutral at 1698.`
- `-    set_default_env("DIALOG_GCD_WIDTH_MARGIN", "26");`
- `+    // ROUND84 x-tail square: Karatsuba beats schoolbook by -16,272 emitted`
- `+    // Toffoli on the peak-1572 base, and Karatsuba's z1_reg fits UNDER the`
- `+    // (WIDTH_MARGIN=27, REROLL=0). Validated 0/0/0 over 9024.`
- `+    // ROUND84_XTAIL_KARATSUBA=0 (+ROUND84_XTAIL_SCHOOLBOOK=1) restores schoolbook.`
- `+    set_default_env("ROUND84_XTAIL_KARATSUBA", "1");`
- `+    // Karatsuba x-tail op stream: margin=27 + REROLL=0 lands a clean 9024-shot`
- `+    // island (anupsv's margin=26/REROLL=20 was for the schoolbook stream).`
- `+    set_default_env("DIALOG_GCD_WIDTH_MARGIN", "27");`
- `-    set_default_env("DIALOG_REROLL", "20");`
- `+    // New op stream (windowed apply + Karatsuba x-tail) needs its own clean`
- `+    // Fiat-Shamir island: REROLL=0 with WIDTH_MARGIN=27 validates 0/0/0 over 9024.`
- `+    set_default_env("DIALOG_REROLL", "0");`

## Public Note Excerpt

> No public note fetched for this commit.

## Follow-up Value

- Reuse when exploring: moves the x-tail square/Solinas binder that controls the low-qubit floor; switches or retunes multiplication/square implementation details; tightens active-width envelope/margin for the dialog-GCD body.
- Watch for validation-island coupling: if this commit touched reroll or compare knobs, retest after stacking with other route changes.
