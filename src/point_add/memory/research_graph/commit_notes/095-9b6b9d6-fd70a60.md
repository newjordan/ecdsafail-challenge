# 095 9b6b9d6 / fd70a60

## Identity

- Commit: `9b6b9d6f334a044ba39070a80dadf12c947c665d`
- Submission: `fd70a604-7114-4624-ada6-712be7fa92be`
- Solver/co-author: Epistetechnician
- Date: 2026-06-02T08:04:50Z
- Public metric: score 3161659020 = 1861990 Toffoli x 1698 qubits
- Public diff: -108570120 (-1.01%)

## Inferred Approach

fuses branch predicates or branch-bit updates to save Toffoli; retunes truncated comparison width, trading correctness-island search against Toffoli; ports or applies Gidney-style vented adders for low-qubit arithmetic.

Tags: `apply-clean`, `compare-width`, `dialog-gcd`, `fused-branch`, `qubit-cut`, `reroll-island`, `venting`, `width-margin`

## Changed Files

- `src/point_add/memory/2026-06-02-fused-branch-bits.md` (+18/-0)
- `src/point_add/mod.rs` (+175/-31)

## Notable Diff Signals

- `+# 2026-06-02 fused branch bits`
- `+Route: dialog-GCD compressed sidecar, compare window 63, reroll 5, measured apply sub.`
- `+- `DIALOG_GCD_FUSED_BRANCH_BITS=1` fuses the branch comparator with the `b0``
- `+- Metrics: 1,861,990 average executed Toffoli, 1,698 peak qubits.`
- `+- `DIALOG_GCD_APPLY_CLEAN_COMPARE_BITS=1` was classically correct with fusion,`
- `+    if kal_vent_modadd_enabled() {`
- `+    let carries = b.alloc_qubits(n);`
- `+fn dialog_gcd_fused_branch_bits_enabled() -> bool {`
- `+    std::env::var("DIALOG_GCD_FUSED_BRANCH_BITS")`
- `+fn dialog_gcd_ccx_cmp_gt_truncated_into_width(`
- `+    compare_bits: usize,`
- `+    let compare_bits = compare_bits.min(u.len()).max(1);`
- `+    let start = u.len() - compare_bits;`
- `-        dialog_gcd_cmp_gt_truncated_into_width(b, u_active, v_active, cmp, compare_bits);`
- `-        dialog_gcd_cmp_gt_truncated_into_width(b, u_active, v_active, cmp, compare_bits);`
- `+        if dialog_gcd_fused_branch_bits_enabled() {`
- `+            dialog_gcd_ccx_cmp_gt_truncated_into_width(`
- `+                compare_bits,`
- `+            dialog_gcd_cmp_gt_truncated_into_width(b, u_active, v_active, cmp, compare_bits);`
- `+            dialog_gcd_cmp_gt_truncated_into_width(b, u_active, v_active, cmp, compare_bits);`
- `-        dialog_gcd_cmp_gt_truncated_into_width(b, u_active, v_active, cmp, compare_bits);`
- `-        dialog_gcd_cmp_gt_truncated_into_width(b, u_active, v_active, cmp, compare_bits);`
- `+        if dialog_gcd_fused_branch_bits_enabled() {`
- `+            dialog_gcd_ccx_cmp_gt_truncated_into_width(`

## Public Note Excerpt

> Fused the dialog-GCD branch-bit comparator so b0 & b1 is emitted directly under the control, avoiding the intermediate compare qubit toggle/uncompute cost. Local no-env harness proof: 9024/9024 shots OK, 0 classical mismatches, 0 phase-garbage batches, 0 ancilla-garbage batches. Metrics: 1,861,990 average executed Toffoli, 1,698 peak qubits, score 3,161,659,020.

## Follow-up Value

- Reuse when exploring: fuses branch predicates or branch-bit updates to save Toffoli; retunes truncated comparison width, trading correctness-island search against Toffoli; ports or applies Gidney-style vented adders for low-qubit arithmetic.
- Watch for validation-island coupling: if this commit touched reroll or compare knobs, retest after stacking with other route changes.
