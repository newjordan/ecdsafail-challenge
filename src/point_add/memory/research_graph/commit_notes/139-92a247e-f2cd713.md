# 139 92a247e / f2cd713

## Identity

- Commit: `92a247ec59074b4a40154d2ad6f6ef8c1b8a66fe`
- Submission: `f2cd7132-573c-4a2a-9891-1d62310799a9`
- Solver/co-author: mpjunior92
- Date: 2026-06-02T20:21:08Z
- Public metric: score 2539526878 = 1732283 Toffoli x 1466 qubits
- Public diff: -35464622 (-0.33%)

## Inferred Approach

hosts comparator carry/scratch on already clean slices to move the peak binder; materializes ctrl-and-source in chunks and clears each chunk to reduce apply-phase peak; moves the x-tail square/Solinas binder that controls the low-qubit floor.

Tags: `active-iters`, `branch-hosting`, `chunked-apply`, `compare-width`, `dialog-gcd`, `fused-branch`, `karatsuba`, `qubit-cut`, `reroll-island`, `round84-square`, `venting`, `width-margin`

## Changed Files

- `src/point_add/mod.rs` (+191/-33)

## Notable Diff Signals

- `+fn dialog_gcd_branch_bits_host_comparator_enabled() -> bool {`
- `+    std::env::var("DIALOG_GCD_BRANCH_BITS_HOST_COMPARATOR")`
- `+/// Truncated controlled branch-bit comparator that hosts its borrow `c_in` +`
- `+/// allocation would otherwise consume at the branch_bits instant. Falls back to`
- `+/// behaviour is identical to `dialog_gcd_ccx_cmp_gt_truncated_into_width` in that`
- `+fn dialog_gcd_ccx_cmp_gt_truncated_into_width_hosted(`
- `+    compare_bits: usize,`
- `+    let compare_bits = compare_bits.min(u.len()).max(1);`
- `+    let start = u.len() - compare_bits;`
- `+            let borrowed_carries = dialog_gcd_compressed_sidecar_future_carry_slice(`
- `-                dialog_gcd_ccx_cmp_gt_truncated_into_width(`
- `-                    compare_bits,`
- `+                // here would add a dead live-qubit at the branch_bits peak instant`
- `+                // so it is allocated only on the non-fused branch below.`
- `+                if dialog_gcd_branch_bits_host_comparator_enabled() {`
- `+                    // Host the comparator's c_in+carries transient on the idle`
- `+                    // it is unwritten at the comparator instant) so branch_bits no`
- `+                    dialog_gcd_ccx_cmp_gt_truncated_into_width_hosted(`
- `+                        compare_bits,`
- `+                    dialog_gcd_ccx_cmp_gt_truncated_into_width(`
- `+                        compare_bits,`
- `-            let borrowed_carries = dialog_gcd_compressed_sidecar_future_carry_slice(`
- `-                dialog_gcd_ccx_cmp_gt_truncated_into_width(`
- `-                    compare_bits,`

## Public Note Excerpt

> # Peak 1500 -> 1466 (-34q): host the GCD branch comparator carry, then sink the apply
> **Score: 2,539,526,878** (avg-executed Toffoli **1,732,283** x peak **1466** qubits),
> validated clean over all 9024 Fiat-Shamir shots (0 classical / 0 phase / 0 ancilla).
> Base: current best peak-1500 route (`250774e` 10d9e / `7c29f79` mmurrs lineage: the
> 6-co-binder ROUND84/apply teardown - `KARA_Z02_LOWQ`, `KARA_SOL_MOD_VENT`,
> `DIALOG_GCD_APPLY_CHUNKED_F_CUT=99`). Delta ~ **-35M (-1.4%)**, a peak-qubit cut from 1500 to
> 1466 (first sub-1500 / first 1466).
> ## The win - unbind the GCD core, then let the apply chunk sink the global peak
> On the 1500 floor the peak was a co-binder tie between the GCD-core branch comparator
> (`dialog_gcd_compressed_block_tobitvector_branch_bits` / `_reverse`) and the apply mod

## Follow-up Value

- Reuse when exploring: hosts comparator carry/scratch on already clean slices to move the peak binder; materializes ctrl-and-source in chunks and clears each chunk to reduce apply-phase peak; moves the x-tail square/Solinas binder that controls the low-qubit floor.
- Watch for validation-island coupling: if this commit touched reroll or compare knobs, retest after stacking with other route changes.
