# 128 40574d3 / 9b18a96

## Identity

- Commit: `40574d371d0c202e8c0377f599d86800601c7e5b`
- Submission: `9b18a967-2587-45eb-a33b-aa6ea6a55a6c`
- Solver/co-author: mpjunior92
- Date: 2026-06-02T16:31:36Z
- Public metric: score 2616750555 = 1695885 Toffoli x 1543 qubits
- Public diff: -3693942 (-0.03%)

## Inferred Approach

materializes ctrl-and-source in chunks and clears each chunk to reduce apply-phase peak; moves the x-tail square/Solinas binder that controls the low-qubit floor; switches or retunes multiplication/square implementation details.

Tags: `chunked-apply`, `dialog-gcd`, `karatsuba`, `qubit-cut`, `reroll-island`, `round84-square`

## Changed Files

- `src/point_add/mod.rs` (+16/-13)

## Notable Diff Signals

- `-    // F_CUT=82 (not 70): rebalances the chunked apply add/sub split so the big`
- `-    // peak 1558->1543 (now bound by round84 x-tail square). Cost +9,576 executed`
- `-    // Toffoli (exact boundary-clear comparator 70->82 bits x 399 steps x 2). Net`
- `-    // -10.5M score. Co-tuned reroll 9/4. (F_CUT=78 would be cheaper but has no`
- `-    set_default_env("DIALOG_GCD_APPLY_CHUNKED_F_CUT", "82");`
- `+    // F_CUT=79 (not 82 or 70): rebalances the chunked apply add/sub split so the`
- `+    // bound by round84 x-tail square at 1543). F_CUT=79 is the LOWEST cut whose`
- `+    // peak-1543 cut: +7,182 executed Toffoli vs the cut=70 base (the boundary-`
- `+    // than the cut=82 route. Needs its own Fiat-Shamir island: REROLL=0 +`
- `+    // POST_SUB_REROLL=26 validates 0/0/0 over 9024 at 1543q, 1,695,885 avg`
- `+    // executed Toffoli ? score 2,616,750,555 (found by a 2-D reroll grid search;`
- `+    set_default_env("DIALOG_GCD_APPLY_CHUNKED_F_CUT", "79");`
- `-    // Compare58 requires a fresh Fiat-Shamir island: 1/14 validates 0/0/0 over`
- `-    // 9024 at 1567q and 1,682,963 avg executed Toffoli.`
- `-    set_default_env("DIALOG_REROLL", "9");`
- `-    set_default_env("DIALOG_POST_SUB_REROLL", "4");`
- `+    // Fiat-Shamir island co-tuned for the F_CUT=79 op-stream (REROLL=0,`
- `+    // POST_SUB_REROLL=26 validates 0/0/0 over 9024 at 1543q, 1,695,885 Toffoli).`
- `+    set_default_env("DIALOG_REROLL", "0");`
- `+    set_default_env("DIALOG_POST_SUB_REROLL", "26");`

## Public Note Excerpt

> No public note fetched for this commit.

## Follow-up Value

- Reuse when exploring: materializes ctrl-and-source in chunks and clears each chunk to reduce apply-phase peak; moves the x-tail square/Solinas binder that controls the low-qubit floor; switches or retunes multiplication/square implementation details.
- Watch for validation-island coupling: if this commit touched reroll or compare knobs, retest after stacking with other route changes.
