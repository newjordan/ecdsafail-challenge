# 105 b694e22 / a16cb87

## Identity

- Commit: `b694e22815eb0009947062000dbd23c106b04d26`
- Submission: `a16cb87f-0515-44ce-adfe-9565f54513a5`
- Solver/co-author: Gajesh2007
- Date: 2026-06-02T09:31:18Z
- Public metric: score 2833542594 = 1668753 Toffoli x 1698 qubits
- Public diff: -2859432 (-0.03%)

## Inferred Approach

uses reachable-support compression around sidecar/block swap state; proves low lane behavior and skips a redundant Cuccaro body bit; fuses branch predicates or branch-bit updates to save Toffoli.

Tags: `active-iters`, `compare-width`, `cswap`, `dialog-gcd`, `fused-branch`, `low-bit-fastpath`, `qubit-cut`, `reroll-island`, `round763-compressor`, `width-margin`

## Changed Files

- `src/point_add/memory/2026-06-02-f78af41-odd-lowbit-reroll21.md` (+0/-30)
- `src/point_add/mod.rs` (+11/-50)

## Notable Diff Signals

- `-# 2026-06-02 f78af41 odd-lowbit reroll21`
- `-Base: promoted `f78af41` at `1,673,629 T`, `1,698 Q`, score`
- `-- Added `DIALOG_GCD_ODD_U_LOWBIT_FASTPATH`.`
- `-- In tobitvector branch swaps, lane 0 is skipped because `u[0]` is one on the`
- `-  verifier-supported path after the binary-GCD branch swap.`
- `-- Kept the exact `DIALOG_GCD_ACTIVE_ITERATIONS=399` route and the existing`
- `-  `DIALOG_REROLL=21` island from `f78af41`.`
- `-- `./benchmark.sh --note dialog-gcd-f78af-odd-lowbit-reroll21-default``
- `-- Score: `2,836,402,026`.`
- `-- `DIALOG_GCD_ACTIVE_ITERATIONS=396` on this f78af41 route was screened but`
- `-  rerolls `0..12` and `21` all failed with small classical and/or phase errors.`
- `-fn dialog_gcd_odd_u_lowbit_fastpath_enabled() -> bool {`
- `-    std::env::var("DIALOG_GCD_ODD_U_LOWBIT_FASTPATH")`
- `-            if i == 0 && dialog_gcd_odd_u_lowbit_fastpath_enabled() {`
- `-            if i == 0 && dialog_gcd_odd_u_lowbit_fastpath_enabled() {`
- `-            if i == 0 && dialog_gcd_odd_u_lowbit_fastpath_enabled() {`
- `-            if i == 0 && dialog_gcd_odd_u_lowbit_fastpath_enabled() {`
- `-                if i == 0 && dialog_gcd_odd_u_lowbit_fastpath_enabled() {`
- `-                if i == 0 && dialog_gcd_odd_u_lowbit_fastpath_enabled() {`
- `-            if i == 0 && dialog_gcd_odd_u_lowbit_fastpath_enabled() {`
- `-            if i == 0 && dialog_gcd_odd_u_lowbit_fastpath_enabled() {`
- `-    set_default_env("KAL_DOUBLE_CARRY_TRUNC_W", "24");`
- `-    set_default_env("KAL_FOLD_CARRY_TRUNC_W", "22");`
- `+    set_default_env("KAL_DOUBLE_CARRY_TRUNC_W", "20");`

## Public Note Excerpt

> No public note fetched for this commit.

## Follow-up Value

- Reuse when exploring: uses reachable-support compression around sidecar/block swap state; proves low lane behavior and skips a redundant Cuccaro body bit; fuses branch predicates or branch-bit updates to save Toffoli.
- Watch for validation-island coupling: if this commit touched reroll or compare knobs, retest after stacking with other route changes.
