# 142 8ab5f90 / 53cab17

## Identity

- Commit: `8ab5f90ec6b4ac9b182df200c96cfa8f281f3bab`
- Submission: `53cab17b-66d0-497e-b1cb-c296e8c05606`
- Solver/co-author: pldallairedemers
- Date: 2026-06-02T21:59:33Z
- Public metric: score 2511992646 = 1737201 Toffoli x 1446 qubits
- Public diff: -21585204 (-0.20%)

## Inferred Approach

lifetime/hosting optimization that borrows future-clean lanes instead of allocating full scratch; hosts comparator carry/scratch on already clean slices to move the peak binder; materializes ctrl-and-source in chunks and clears each chunk to reduce apply-phase peak.

Tags: `active-iters`, `apply-clean`, `branch-hosting`, `chunked-apply`, `compare-width`, `dialog-gcd`, `low-bit-fastpath`, `partial-hosting`, `qubit-cut`, `reroll-island`, `round218-b5`

## Changed Files

- `src/point_add/mod.rs` (+60/-85)

## Notable Diff Signals

- `+fn dialog_gcd_materialized_body_gated_register<'a>(`
- `+    let borrowed_for_body = if dialog_gcd_host_gated_enabled() {`
- `+    let hosted_gated_len = borrowed_for_body`
- `+    let owned_gated = b.alloc_qubits(body_len - hosted_gated_len);`
- `+        gated.extend_from_slice(&carries[carry_need..carry_need + hosted_gated_len]);`
- `-        // Host the gated register on the tail of the borrowed clean slice when`
- `-        let gated_host: Option<&[QubitId]> = if dialog_gcd_host_gated_enabled() {`
- `-        let gated: &[QubitId] = match gated_host {`
- `-                gated_owned = b.alloc_qubits(n);`
- `+            dialog_gcd_materialized_body_gated_register(b, body_len, borrowed_carries);`
- `-        if gated_host.is_none() {`
- `-        let gated_host: Option<&[QubitId]> = if dialog_gcd_host_gated_enabled() {`
- `-        let gated: &[QubitId] = match gated_host {`
- `-                gated_owned = b.alloc_qubits(n);`
- `+            dialog_gcd_materialized_body_gated_register(b, body_len, borrowed_carries);`
- `-        if gated_host.is_none() {`
- `-    // Branch comparator width tightened 58 -> 57 on the 1466 hosted-comparator`
- `-    // avg-executed Toffoli). Composed on active398 it re-rolls the FS island,`
- `-    set_default_env("DIALOG_GCD_COMPARE_BITS", "57");`
- `+    set_default_env("DIALOG_GCD_COMPARE_BITS", "58");`
- `-    set_default_env("DIALOG_GCD_APPLY_CHUNKED_F_CUT", "116");`
- `-    // T-cut on the 1466q hosted-comparator route: the active398 island composes`
- `-    // with one more branch-compare bit cut. A 2-D reroll search lands 52/28`
- `-    // clean 0/0/0 over all 9024 shots at 1466q and 1,729,241 Toffoli.`

## Public Note Excerpt

> # Partial body-gated hosting: 1446q point-addition route
> Base: promoted `14b84f1` / `700582e`, score `2,533,577,850`
> (`Q=1466`, `T=1,728,225`).
> This submission adds a value-exact lifetime/hosting cut in the materialized
> dialog-GCD body. The existing route can host the whole `gated` register only
> when the future-log clean slice has `carry(n-1) + gated(n)` lanes available.
> Near the peak that is often too strict, so the old code falls back to allocating
> the entire body-wide gated register.
> The new helper hosts the prefix of the body-local gated register that fits after
> the borrowed carry prefix and allocates only the remaining gated lanes. It uses

## Follow-up Value

- Reuse when exploring: lifetime/hosting optimization that borrows future-clean lanes instead of allocating full scratch; hosts comparator carry/scratch on already clean slices to move the peak binder; materializes ctrl-and-source in chunks and clears each chunk to reduce apply-phase peak.
- Watch for validation-island coupling: if this commit touched reroll or compare knobs, retest after stacking with other route changes.
