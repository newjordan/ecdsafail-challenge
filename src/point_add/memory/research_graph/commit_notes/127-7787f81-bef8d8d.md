# 127 7787f81 / bef8d8d

## Identity

- Commit: `7787f814bf92170e663c25aeb65a65297af483a0`
- Submission: `bef8d8da-46a4-4ff8-84d1-0e81bfc5d4a4`
- Solver/co-author: mpjunior92
- Date: 2026-06-02T16:22:57Z
- Public metric: score 2620444497 = 1698279 Toffoli x 1543 qubits
- Public diff: -10554777 (-0.10%)

## Inferred Approach

materializes ctrl-and-source in chunks and clears each chunk to reduce apply-phase peak; moves the x-tail square/Solinas binder that controls the low-qubit floor; uses reachable-support compression around sidecar/block swap state.

Tags: `chunked-apply`, `compare-width`, `dialog-gcd`, `karatsuba`, `qubit-cut`, `reroll-island`, `round763-compressor`, `round84-square`, `width-margin`

## Changed Files

- `src/point_add/mod.rs` (+11/-3)

## Notable Diff Signals

- `-    set_default_env("DIALOG_GCD_APPLY_CHUNKED_F_CUT", "70");`
- `+    // F_CUT=82 (not 70): rebalances the chunked apply add/sub split so the big`
- `+    // peak 1558->1543 (now bound by round84 x-tail square). Cost +9,576 executed`
- `+    // Toffoli (exact boundary-clear comparator 70->82 bits x 399 steps x 2). Net`
- `+    // -10.5M score. Co-tuned reroll 9/4. (F_CUT=78 would be cheaper but has no`
- `+    set_default_env("DIALOG_GCD_APPLY_CHUNKED_F_CUT", "82");`
- `-    set_default_env("DIALOG_REROLL", "13");`
- `-    set_default_env("DIALOG_POST_SUB_REROLL", "14");`
- `+    set_default_env("DIALOG_REROLL", "9");`
- `+    set_default_env("DIALOG_POST_SUB_REROLL", "4");`

## Public Note Excerpt

> # Apply chunked add/sub: rebalance F_CUT 70->82 (peak 1558 -> 1543)
> **Score: 2,620,444,497** (avg-executed Toffoli **1,698,279** x peak **1543** qubits),
> validated clean over all 9024 Fiat-Shamir shots (0 classical / 0 phase / 0 ancilla).
> Base: best promoted **44155bb4** (b113c6f, 2,630,999,274, Q=1558, T=1,688,703).
> Delta = **-10,554,777 (-0.40%)**, a peak-qubit cut.
> ## The win
> After the previous round84 doublings fix dropped the peak to 1558, the binder became
> the apply-step chunked modular add/sub (`dialog_gcd_..._chunked_raw_difference` /
> `_raw_sum`). Its peak instant = base(~1186) + the materialized `f` chunk (`ctrl &
> source` for the big chunk) + the Cuccaro carry lane = **base + 2?(big-chunk width)**.

## Follow-up Value

- Reuse when exploring: materializes ctrl-and-source in chunks and clears each chunk to reduce apply-phase peak; moves the x-tail square/Solinas binder that controls the low-qubit floor; uses reachable-support compression around sidecar/block swap state.
- Watch for validation-island coupling: if this commit touched reroll or compare knobs, retest after stacking with other route changes.
