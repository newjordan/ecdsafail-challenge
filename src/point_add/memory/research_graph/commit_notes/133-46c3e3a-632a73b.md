# 133 46c3e3a / 632a73b

## Identity

- Commit: `46c3e3a4756fccfd454c827da40d73793b0ca45e`
- Submission: `632a73b6-67b0-4a60-8756-3af8525acbab`
- Solver/co-author: bxue-l2
- Date: 2026-06-02T17:57:38Z
- Public metric: score 2593889178 = 1682159 Toffoli x 1542 qubits
- Public diff: -1682159 (-0.02%)

## Inferred Approach

moves the x-tail square/Solinas binder that controls the low-qubit floor; switches or retunes multiplication/square implementation details; tightens active-width envelope/margin for the dialog-GCD body.

Tags: `karatsuba`, `qubit-cut`, `reroll-island`, `round84-square`, `width-margin`

## Changed Files

- `src/point_add/mod.rs` (+29/-5)

## Notable Diff Signals

- `-    let z1_reg = b.alloc_qubits(2 * (h + 1));`
- `+    let mut z1_reg = b.alloc_qubits(2 * (h + 1));`
- `-        let pad = b.alloc_qubits(3 * h - 2 * (h + 1));`
- `+        let pad = b.alloc_qubits(3 * h - z1_reg.len());`
- `-        let pad = b.alloc_qubits(3 * h - 2 * (h + 1));`
- `+        let pad = b.alloc_qubits(3 * h - z1_reg.len());`
- `+    // ROUND84 Karatsuba z1_reg top bit (index 257) is provably 0 across the whole`
- `+    // combine restores z1=(lo+hi)^2. Bennett-clean, 0 added Toffoli. Stacks on`
- `+    // REROLL=17/POST_SUB=56 below (MARGIN stays 5 - no give-back). Validated 0/0/0`
- `+    set_default_env("KARA_FREE_Z1_TOPBIT", "1");`
- `-    set_default_env("DIALOG_REROLL", "40");`
- `-    set_default_env("DIALOG_POST_SUB_REROLL", "13");`
- `+    set_default_env("DIALOG_REROLL", "17");`
- `+    set_default_env("DIALOG_POST_SUB_REROLL", "56");`

## Public Note Excerpt

> No public note fetched for this commit.

## Follow-up Value

- Reuse when exploring: moves the x-tail square/Solinas binder that controls the low-qubit floor; switches or retunes multiplication/square implementation details; tightens active-width envelope/margin for the dialog-GCD body.
- Watch for validation-island coupling: if this commit touched reroll or compare knobs, retest after stacking with other route changes.
