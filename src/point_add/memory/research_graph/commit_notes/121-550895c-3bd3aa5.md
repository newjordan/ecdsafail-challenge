# 121 550895c / 3bd3aa5

## Identity

- Commit: `550895c35f3ca389783f31887806ffb32791d3a0`
- Submission: `3bd3aa5a-c36a-432e-92af-c4933a860a80`
- Solver/co-author: solimander
- Date: 2026-06-02T13:24:52Z
- Public metric: score 2647454335 = 1689505 Toffoli x 1567 qubits
- Public diff: -84484816 (-0.79%)

## Inferred Approach

materializes ctrl-and-source in chunks and clears each chunk to reduce apply-phase peak; moves the x-tail square/Solinas binder that controls the low-qubit floor; uses reachable-support compression around sidecar/block swap state.

Tags: `apply-clean`, `chunked-apply`, `compare-width`, `dialog-gcd`, `qubit-cut`, `reroll-island`, `round763-compressor`, `round84-square`

## Changed Files

- `src/point_add/memory/2026-06-02-chunked-apply-f.md` (+28/-0)
- `src/point_add/mod.rs` (+267/-110)

## Notable Diff Signals

- `+# 2026-06-02 Chunked Apply F`
- `+the raw apply ripple, `DIALOG_GCD_APPLY_CHUNKED_F_BLOCKS=2` loads one slice at`
- `+The winning route uses `DIALOG_GCD_APPLY_CHUNKED_F_CUT=70`, which keeps the raw`
- `+apply phases below the existing ROUND84 peak while shrinking the boundary`
- `+- `DIALOG_GCD_APPLY_CHUNKED_F_BLOCKS=2``
- `+- `DIALOG_GCD_APPLY_CHUNKED_F_CUT=70``
- `+- `DIALOG_REROLL=4``
- `+- `DIALOG_POST_SUB_REROLL=15``
- `+- Trace/eval: `1567` qubits, `1,689,505` average executed Toffoli,`
- `+  score `2,647,954,335`, all `9024` shots OK.`
- `+- `cut=68`: `1567q`, `1,687,909T`, but first seed failed and needs reroll.`
- `+- `cut=70`: `1567q`, `1,689,505T`, clean at reroll `4/15`.`
- `+fn dialog_gcd_apply_chunked_f_blocks() -> Option<usize> {`
- `+    std::env::var("DIALOG_GCD_APPLY_CHUNKED_F_BLOCKS")`
- `+fn dialog_gcd_apply_chunked_f_cut() -> Option<usize> {`
- `+    std::env::var("DIALOG_GCD_APPLY_CHUNKED_F_CUT")`
- `+    if let Some(blocks) = dialog_gcd_apply_chunked_f_blocks()`
- `+        .filter(|_| dialog_gcd_raw_apply_truncated_clean_enabled())`
- `+        dialog_gcd_cmod_add_materialized_pseudomersenne_chunked(b, acc, a, ctrl, p, blocks);`
- `+fn dialog_gcd_clean_truncated_underflow(`
- `+    let compare_start = N - dialog_gcd_apply_clean_compare_bits();`
- `+    for &q in &a[compare_start..] {`
- `+    ccx_cmp_lt_into_fast(b, &acc[compare_start..], &a[compare_start..], ctrl, acc_ovf);`
- `+    for &q in &a[compare_start..] {`

## Public Note Excerpt

> # Chunked apply materialization with asymmetric cut
> This submission reduces the apply-phase peak by avoiding a full 256-bit
> materialized `f = ctrl & a` during the raw add/sub ripple.
> Changes:
> - Added an env-gated chunked materialization path for the dialog-GCD apply
>   modular add/sub helpers.
> - The default route now uses `DIALOG_GCD_APPLY_CHUNKED_F_BLOCKS=2` with
>   `DIALOG_GCD_APPLY_CHUNKED_F_CUT=70`.
> - Each chunk loads only the active `ctrl & a` slice, runs the corresponding
>   Cuccaro block, then clears the slice with HMR.

## Follow-up Value

- Reuse when exploring: materializes ctrl-and-source in chunks and clears each chunk to reduce apply-phase peak; moves the x-tail square/Solinas binder that controls the low-qubit floor; uses reachable-support compression around sidecar/block swap state.
- Watch for validation-island coupling: if this commit touched reroll or compare knobs, retest after stacking with other route changes.
