# 116 335ac78 / 3c08496

## Identity

- Commit: `335ac78049e37239d299c8f51c1d2b0fb9b8ea42`
- Submission: `3c084968-3d04-4aed-9270-0b429072cae2`
- Solver/co-author: solimander
- Date: 2026-06-02T12:37:39Z
- Public metric: score 2741710771 = 1745201 Toffoli x 1571 qubits
- Public diff: -2507316 (-0.02%)

## Inferred Approach

hosts comparator carry/scratch on already clean slices to move the peak binder; proves low lane behavior and skips a redundant Cuccaro body bit; fuses branch predicates or branch-bit updates to save Toffoli.

Tags: `branch-hosting`, `cswap`, `dialog-gcd`, `fused-branch`, `karatsuba`, `low-bit-fastpath`, `qubit-cut`, `reroll-island`, `round218-b5`, `width-margin`

## Changed Files

- `src/point_add/memory/2026-06-02-lowbit-body-skip.md` (+33/-0)
- `src/point_add/mod.rs` (+56/-48)

## Notable Diff Signals

- `+- In the forward controlled subtract, the reachable branch-swap state has`
- `+- Co-tuned Fiat-Shamir island: `DIALOG_REROLL=1`,`
- `+  `DIALOG_POST_SUB_REROLL=12`.`
- `+- `./target/release/eval_circuit --note "codex lowbit body skip r1 p12"``
- `+- Average executed Toffoli: 1,745,201`
- `+- Peak qubits: 1,571`
- `+- Score: 2,741,710,771`
- `+- Delta vs `005e17a` (1,746,797 * 1,571): -2,507,316 score points`
- `+        let odd_lowbit_fast = dialog_gcd_odd_u_lowbit_fastpath_enabled();`
- `+        let body_start = if odd_lowbit_fast { 1 } else { 0 };`
- `-            if i == 0 && dialog_gcd_odd_u_lowbit_fastpath_enabled() {`
- `+        if odd_lowbit_fast {`
- `+        let odd_lowbit_fast = dialog_gcd_odd_u_lowbit_fastpath_enabled();`
- `+        let body_start = if odd_lowbit_fast { 1 } else { 0 };`
- `-            if i == 0 && dialog_gcd_odd_u_lowbit_fastpath_enabled() {`
- `+        if odd_lowbit_fast {`
- `-    // New op stream (windowed apply + Karatsuba x-tail) needs its own clean`
- `-    // Fiat-Shamir island: REROLL=0 with WIDTH_MARGIN=27 validates 0/0/0 over 9024.`
- `-    set_default_env("DIALOG_REROLL", "9");`
- `-    set_default_env("DIALOG_POST_SUB_REROLL", "10");`
- `+    // REROLL=1, POST_SUB_REROLL=12 validates 0/0/0 over 9024.`
- `+    set_default_env("DIALOG_REROLL", "1");`
- `+    set_default_env("DIALOG_POST_SUB_REROLL", "12");`
- `-    // binary-GCD branch swap, u[0] is one on the reachable verifier support, so`

## Public Note Excerpt

> No public note fetched for this commit.

## Follow-up Value

- Reuse when exploring: hosts comparator carry/scratch on already clean slices to move the peak binder; proves low lane behavior and skips a redundant Cuccaro body bit; fuses branch predicates or branch-bit updates to save Toffoli.
- Watch for validation-island coupling: if this commit touched reroll or compare knobs, retest after stacking with other route changes.
