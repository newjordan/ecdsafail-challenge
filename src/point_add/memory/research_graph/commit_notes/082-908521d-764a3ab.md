# 082 908521d / 764a3ab

## Identity

- Commit: `908521df810f688a548b3defbdd3c0dfa3d284db`
- Submission: `764a3ab3-e98b-406f-bca9-7edec872e789`
- Solver/co-author: anupsv
- Date: 2026-06-02T05:40:54Z
- Public metric: score 5121484498 = 2553083 Toffoli x 2006 qubits
- Public diff: -27465308 (-0.26%)

## Inferred Approach

tightens active-width envelope/margin for the dialog-GCD body; retunes Fiat-Shamir reroll knobs to find a clean validation island after exact route changes; primarily targets peak qubit width rather than raw Toffoli.

Tags: `cswap`, `qubit-cut`, `reroll-island`, `width-margin`

## Changed Files

- `src/point_add/kaliski_state.rs` (+11/-26)
- `src/point_add/mod.rs` (+11/-7)
- `src/point_add/mul_affine.rs` (+9/-7)

## Notable Diff Signals

- `-// 2^iter), netting -1,248 avg-exec Toffoli. The op-count change re-rolls the`
- `-// shots). The baked KAL_REROLL default (=34, see mod.rs) is CO-TUNED to this`
- `-// threshold; re-search if any scored op changes the op stream.`
- `-    // K0=21, shaving ~4,400 avg-exec Toffoli across step2/3/4) NOW lands a clean`
- `-    // x 2002 peak = 5,148,949,806, screened 0/0/0. The baked KAL_REROLL default`
- `-    // scored-op change.`
- `-    // island (mod.rs KAL_REROLL), so slack=0 is correct on all 9024 eval shots and`
- `-    // 0/0/0, score 5,160,257,102). Re-search rr if any scored op changes. Override remains.`
- `-    env_usize("KAL_CSWAP_UV_MERGE_SAFE_ITERS").unwrap_or(254)`
- `+    // cswaps fwd+bwd, -22,600 avg-exec Toffoli) co-tuned with reroll=5 below.`
- `+    // 330 has no clean reroll island (u==v_w equality at iter 329 breaks the`
- `+    env_usize("KAL_CSWAP_UV_MERGE_SAFE_ITERS").unwrap_or(329)`
- `-        // Re-search this value whenever any scored op (slack/mfw/truncation/K0 knob /`
- `+        // value whenever any scored op (truncation knob / structural edit) changes`
- `+        // rr=5 is CO-TUNED to the safe_iters=329 op stream (uv-cswap merge extended`
- `+        // square ceiling (most fast measurement-Cuccaro rows / least Toffoli) that`
- `+        // Toffoli than 234 for no peak benefit.)`

## Public Note Excerpt

> No public note fetched for this commit.

## Follow-up Value

- Reuse when exploring: tightens active-width envelope/margin for the dialog-GCD body; retunes Fiat-Shamir reroll knobs to find a clean validation island after exact route changes; primarily targets peak qubit width rather than raw Toffoli.
- Watch for validation-island coupling: if this commit touched reroll or compare knobs, retest after stacking with other route changes.
