# 023 d19dbb5 / f94f726

## Identity

- Commit: `d19dbb5390c11aaaac70e113647dbe07530a5c41`
- Submission: `f94f726c-39d3-48fc-af71-81c0d808f631`
- Solver/co-author: Gajesh2007
- Date: 2026-05-31T20:05:06Z
- Public metric: score 8445450090 = 3656039 Toffoli x 2310 qubits
- Public diff: -1145844506 (-10.65%)

## Inferred Approach

moves the x-tail square/Solinas binder that controls the low-qubit floor; ports or applies Gidney-style vented adders for low-qubit arithmetic; switches or retunes multiplication/square implementation details.

Tags: `cswap`, `karatsuba`, `qubit-cut`, `round84-square`, `venting`, `width-margin`

## Changed Files

- `src/point_add/mod.rs` (+1295/-380)

## Notable Diff Signals

- `+///     CCX/CX, no measurement-vent).`
- `+/// Sub-knob: late-iter Toffoli RECOVERY - widen the clean carry-borrow pool of`
- `+/// Background: the 9n-floor (KAL_GOUZIEN_9N) hosts the fast-Cuccaro carry`
- `+/// it costs +~133k Toffoli (the +3.72% the 9n-floor paid for the 2333 peak).`
- `+/// `u[2n-iter_idx..n)` (= iter_idx-n qubits) are PROVABLY |0>. They are already`
- `+/// function of iter_idx (no data-dependent branch), so validity is structural.`
- `+/// STEP-4 conditional const-sub (the 2333 binder) with Gidney venting`
- `+/// k-bit spill add plus a venting dirty-borrow controlled-increment. Both cut`
- `+/// ~256 clean transient qubits at the affine y-mul Solinas-fold binder.`
- `+///   - pair1_mul1 + pair2_mul use schoolbook (no Karatsuba z1_reg) so the mul`
- `+/// Net: global peak 2708 -> 2565, score 9.604e9 -> 9.361e9 (-11.34% vs baseline).`
- `+    // Stack default: pair1_mul1 is schoolbook (frees the Karatsuba z1_reg from`
- `+    // the binding sol_sub6 boundary). User can force Karatsuba via the knob.`
- `+    let karatsuba_default = !stack_2565_enabled();`
- `-        && env_flag_enabled("KAL_PAIR1_MUL1_KARATSUBA", true)`
- `+        && env_flag_enabled("KAL_PAIR1_MUL1_KARATSUBA", karatsuba_default)`
- `+    // Stack default: pair2_mul is schoolbook (frees the Karatsuba z1_reg from`
- `+    // the binding kara_z1_add boundary). User can force Karatsuba via the knob.`
- `+    let karatsuba_default = !stack_2565_enabled();`
- `-        && env_flag_enabled("KAL_PAIR2_MUL_KARATSUBA", true)`
- `+        && env_flag_enabled("KAL_PAIR2_MUL_KARATSUBA", karatsuba_default)`
- `+/// n-1 carry qubits are BORROWED from `carry_src` (which MUST be clean |0? on`
- `+/// of freshly allocated. Flat Toffoli, zero new width at the peak - the carry`
- `+/// register is hosted on already-live but idle clean ancilla (e.g. the future`

## Public Note Excerpt

> # secp256k1 reversible point-add - 20.17% below baseline
> **Score 8,445,450,090 = 3,656,039 average-executed Toffoli x 2,310 peak qubits**
> (baseline 10,579,872,520 -> **-20.17%**), validated clean on all 9024 Fiat-Shamir
> shots: 0 classical mismatches, 0 phase-garbage batches, 0 ancilla-garbage batches.
> ## How it was done
> Starting from the Roetteler two-Kaliski affine point-addition, the result is a
> sequence of individually-validated, stacked optimizations across both scored axes
> - Toffoli count and peak qubit width. Each is gated behind a flag and restores the
> prior banked circuit byte-identically when disabled.
> ### Toffoli axis - boundary merges and carry recovery

## Follow-up Value

- Reuse when exploring: moves the x-tail square/Solinas binder that controls the low-qubit floor; ports or applies Gidney-style vented adders for low-qubit arithmetic; switches or retunes multiplication/square implementation details.
- Watch for validation-island coupling: if this commit touched reroll or compare knobs, retest after stacking with other route changes.
