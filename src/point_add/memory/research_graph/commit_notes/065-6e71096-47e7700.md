# 065 6e71096 / 47e7700

## Identity

- Commit: `6e71096b42f45cb095ae9705c3023a2102767b92`
- Submission: `47e77009-ba70-45e1-a108-f340db67e6ac`
- Solver/co-author: Gajesh2007
- Date: 2026-06-02T01:01:42Z
- Public metric: score 5561829149 = 2408761 Toffoli x 2309 qubits
- Public diff: -849712 (-0.01%)

## Inferred Approach

proves low lane behavior and skips a redundant Cuccaro body bit; tightens active-width envelope/margin for the dialog-GCD body; retunes Fiat-Shamir reroll knobs to find a clean validation island after exact route changes.

Tags: `cswap`, `low-bit-fastpath`, `qubit-cut`, `reroll-island`, `width-margin`

## Changed Files

- `src/point_add/kaliski_state.rs` (+40/-5)
- `src/point_add/mod.rs` (+32/-0)
- `src/point_add/point_add.rs` (+3/-10)
- `src/point_add/screen.rs` (+374/-0)
- `src/point_add/solinas.rs` (+1/-1)

## Notable Diff Signals

- `+    // default-seed lottery, but the free KAL_REROLL knob (identity X;X pairs,`
- `+    // zero Toffoli / peak) lands a clean 9024 island at rr=12 - validated 0/0/0,`
- `+    // KAL_REROLL default (=12) is CO-TUNED to this K0; changing either re-rolls`
- `+        // co-tune with KAL_REROLL to land a clean Fiat-Shamir island.`
- `+        // (the comparator/cswap index [0]). Default slope 2/3 bottoms at ~3 and is`
- `+    // Fiat-Shamir island; a 128-reroll screen found clean islands at rr=35 and`
- `+    // validated 0/0/0 over 9024 by official benchmark.sh). The baked KAL_REROLL`
- `+    // rerolls (too aggressive). -1,277 avg-exec Toffoli vs the W=20 baseline.`
- `+    // DEV-ONLY: if KAL_SCREEN is set, run the in-process reroll/knob sweep and`
- `+    // exit. No-op on the scored path (var unset). See screen.rs.`
- `+    // ?? KAL_REROLL: FREE Fiat-Shamir re-roll knob. The test inputs are a`
- `+    // already-live output qubit (identity, zero Toffoli, zero new qubits, no`
- `+    // scored circuit (peak/Toffoli unchanged). This turns the empirical`
- `+        // this value whenever any scored op (truncation knob / structural edit)`
- `+        let rr: usize = std::env::var("KAL_REROLL")`
- `-    // Lottery re-roll: at the cswap-on/W-TRUNC base, the (pair1=397, pair2=396)`
- `-    // lenient input set). Validated 0/0/0, avg-exec 2,409,129 Toffoli x 2309 =`
- `+//! Never runs on the scored path: `maybe_run_screen()` returns immediately`
- `+//! `KAL_REROLL` Fiat-Shamir island lottery (and any truncation knob set in the`
- `+//! env) without the 28s official `benchmark.sh` round-trip per trial.`
- `+//!   * builds the circuit ONCE (the reroll only appends trailing X;X pairs on`
- `+//!     qubit 0, so every reroll shares the same multi-million-op prefix), and`
- `+//!     Fiat-Shamir seed + inputs + simulation for its rerolls; the op prefix is`
- `+/// `xpair` repeated `rr` times (the reroll structure), WITHOUT materializing the`

## Public Note Excerpt

> No public note fetched for this commit.

## Follow-up Value

- Reuse when exploring: proves low lane behavior and skips a redundant Cuccaro body bit; tightens active-width envelope/margin for the dialog-GCD body; retunes Fiat-Shamir reroll knobs to find a clean validation island after exact route changes.
- Watch for validation-island coupling: if this commit touched reroll or compare knobs, retest after stacking with other route changes.
